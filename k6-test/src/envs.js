export const HOST = __ENV.HOST || "localhost";
export const PORT = __ENV.GRPC_PORT || 1025;

export const MAX_VUS = __ENV.MAX_VUS || 600;
export const MAX_ITERATIONS = __ENV.MAX_ITERATIONS || 10000;

export const LOG_ENABLED = __ENV.LOG_ENABLED || "false";
